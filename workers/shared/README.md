# shared

Cross-worker types, db helpers, and constants. Imported via relative paths
from `workers/api`, `workers/matching-do`, `workers/live-channel-do`.

Phase 2 keeps this empty — types currently live alongside the api worker.
